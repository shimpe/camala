<filter id="displacement_${line}" filterUnits="userSpaceOnUse" x="-50%" y="-50%" width="200%" height="200%" color-interpolation-filters="${Animations.Filter.colorInterpolationFilters_${line}}">
	<feTurbulence type="turbulence" baseFrequency="${Animations.Filter.xBaseFrequency_${line}} ${Animations.Filter.yBaseFrequency_${line}}" numOctaves="${Animations.Filter.numOctaves_${line}}" seed="${Animations.Filter.seed_${line}}" stitchTiles="stitch" x="0%" y="0%" width="100%" height="100%" result="turbulence"/>
	<feDisplacementMap in="SourceGraphic" in2="turbulence" scale="${Animations.Filter.scale_${line}}" xChannelSelector="${Animations.Filter.xChannelSelector_${line}}" yChannelSelector="${Animations.Filter.yChannelSelector_${line}}" x="0%" y="0%" width="100%" height="100%" result="displacementMap"/>
</filter>
